<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.parcialclinicapoo">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParcialClinicaPoo"
        tools:targetApi="31">

        <activity android:name=".view.RegistroPacienteActivity" />
        <activity android:name=".view.RegistroMedicoActivity" />
        <activity android:name=".view.ListaPacientesActivity" />
        <activity android:name=".view.ListaMedicosActivity" />
        <activity android:name=".view.HistorialPacienteActivity" />
        <activity android:name=".view.ConsultasMedicoActivity" />
        <activity android:name=".view.AsignarConsultaActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".view.MainAdminActivity" />
        <activity android:name=".view.MainMedicoActivity" />

        <!-- 👇 Login como actividad principal -->
        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>